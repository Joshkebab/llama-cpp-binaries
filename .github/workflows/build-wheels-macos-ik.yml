name: Build macOS Wheels (ik_llama.cpp)

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag of llama-cpp-binaries to build: v0.1.0'
        default: 'v0.1.0'
        required: true
        type: string
  workflow_call:
    inputs:
      version:
        description: 'Version tag of llama-cpp-binaries to build: v0.1.0'
        default: 'v0.1.0'
        required: true
        type: string

permissions:
  contents: write

jobs:
  build_wheels_arm64:
    name: macOS ARM64 Metal (ik)
    runs-on: macos-14

    steps:
      - uses: actions/checkout@v6
        with:
          repository: 'oobabooga/llama-cpp-binaries'
          ref: ${{ inputs.version }}
          submodules: 'recursive'

      - name: Setup ik_llama.cpp
        run: |
          mv llama_cpp_binaries ik_llama_cpp_binaries
          sed -i '' 's/llama_cpp_binaries/ik_llama_cpp_binaries/g' pyproject.toml setup.py

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: |
          python -m pip install build cmake

      - name: Build Wheel
        env:
          LLAMA_CPP_DIR: ik_llama.cpp
        run: |
          export CMAKE_ARGS="-DCMAKE_BUILD_TYPE=Release -DGGML_NATIVE=off -DGGML_METAL=on -DGGML_METAL_EMBED_LIBRARY=on -DGGML_MAX_CONTEXTS=2048 -DGGML_RPC=on -DLLAMA_BUILD_TESTS=off -DLLAMA_BUILD_EXAMPLES=on -DLLAMA_BUILD_SERVER=on"
          export MACOSX_DEPLOYMENT_TARGET="13.3"
          export CMAKE_OSX_ARCHITECTURES="arm64"
          export ARCHFLAGS="-arch arm64"

          VERBOSE=1 python -m build --wheel

          # Rename universal2 wheels to arm64
          for file in ./dist/*universal2.whl; do
            mv "$file" "${file/universal2/arm64}"
          done

      - name: Upload files to a GitHub release
        id: upload-release
        uses: svenstaro/upload-release-action@2.7.0
        continue-on-error: true
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: ./dist/*.whl
          tag: ${{ inputs.version }}
          release_name: ${{ inputs.version }}
          file_glob: true
          make_latest: false
          overwrite: true

      - uses: actions/upload-artifact@v6
        if: steps.upload-release.outcome == 'failure'
        with:
          name: macos-arm64-ik
          path: ./dist/*.whl

  build_wheels_x64:
    name: macOS x64 (ik)
    runs-on: macos-15-intel

    steps:
      - uses: actions/checkout@v6
        with:
          repository: 'oobabooga/llama-cpp-binaries'
          ref: ${{ inputs.version }}
          submodules: 'recursive'

      - name: Setup ik_llama.cpp
        run: |
          mv llama_cpp_binaries ik_llama_cpp_binaries
          sed -i '' 's/llama_cpp_binaries/ik_llama_cpp_binaries/g' pyproject.toml setup.py

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install Dependencies
        run: |
          python -m pip install build cmake

      - name: Build Wheel
        env:
          LLAMA_CPP_DIR: ik_llama.cpp
        run: |
          export CMAKE_ARGS="-DCMAKE_BUILD_TYPE=Release -DGGML_NATIVE=off -DGGML_METAL=off -DGGML_MAX_CONTEXTS=2048 -DGGML_RPC=on -DLLAMA_BUILD_TESTS=off -DLLAMA_BUILD_EXAMPLES=on -DLLAMA_BUILD_SERVER=on"
          export MACOSX_DEPLOYMENT_TARGET="13.3"
          export CMAKE_OSX_ARCHITECTURES="x86_64"
          export ARCHFLAGS="-arch x86_64"

          VERBOSE=1 python -m build --wheel

          # Rename universal2 wheels to x86_64
          for file in ./dist/*universal2.whl; do
            mv "$file" "${file/universal2/x86_64}"
          done

      - name: Upload files to a GitHub release
        id: upload-release
        uses: svenstaro/upload-release-action@2.7.0
        continue-on-error: true
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: ./dist/*.whl
          tag: ${{ inputs.version }}
          release_name: ${{ inputs.version }}
          file_glob: true
          make_latest: false
          overwrite: true

      - uses: actions/upload-artifact@v6
        if: steps.upload-release.outcome == 'failure'
        with:
          name: macos-x64-ik
          path: ./dist/*.whl
